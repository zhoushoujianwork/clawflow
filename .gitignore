/config/
/clawflow
/clawflow_*

# Frontend tool caches. web/dist/ is generated at build time and never
# committed — release CI runs `pnpm build` before `go build`, and local
# `make build` / `make install` chain it via the `web` target.
/web/node_modules/
/web/.vite/
/web/.cache/
/web/dist/
